# Agent Review

Review artifacts produced by agent quality passes. Start at `quality-pass/latest.md` for the current engineering-quality report; `INDEX.md` maps every run to its commit and headline. Reports are committed to the repo in dedicated commits (`agent_review: quality-pass report at <commit>`) — never edit a past report for a commit that is no longer `HEAD`; write a new one.
