# Agent Review — Run Index

Chronological history of all review runs, by review type. Newest last. Each type's `latest.md` is always its current report.

## Skills user-journey reviews (`skills-journey/`)

| Run | Date (IST) | Commit | High | Med | Low | Snapshot |
|---|---|---|---|---|---|---|
| 1 | 2026-06-12 15:56 (+16:01 addendum) | `5e3ffcd` | 2 (SJ1 resumable ideas, SJ2 validation dead end) | 4 (SJ3-SJ5, SJ9) | 4 (SJ6-SJ8, SJ10) | [skills-journey/2026-06-12-5e3ffcd.md](skills-journey/2026-06-12-5e3ffcd.md) |

Verdict (Run 1): run journey fine (1 click); creation journey too complicated in surface + exits, not steps — 3 contract beats drifted to ~7 visible, ~12-14 concepts vs ~4 budgeted; funnel: 21 ideas → 12 stuck `incomplete` → 8 skills (57% structural abandonment). Addendum: live UX test independently confirmed the SJ3/SJ5 discoverability cluster on builtins (all 8 slashes exist; labels ≠ slash names, 4 hidden from autocomplete, unknown slashes silently route to copilot) + 2 new findings (SJ9 "setup matter" alias runs the wrong workflow; SJ10 suggestions never reload after login).

## Engineering quality passes (`quality-pass/`)

| Run | Date (IST) | Commit | P1 | P2 | P3 | Snapshot |
|---|---|---|---|---|---|---|
| 1 | 2026-06-06 11:46 | `8fa0b3e` | 8 | 4 | 16 | [legacy: claude_tests/quality-pass/2026-06-06-run1-8fa0b3e.md](../claude_tests/quality-pass/2026-06-06-run1-8fa0b3e.md) |
| 2 | 2026-06-06 15:18 | `bf6d8fb` | 7 | 3 | 18 | [legacy: claude_tests/quality-pass/2026-06-06-run2-bf6d8fb.md](../claude_tests/quality-pass/2026-06-06-run2-bf6d8fb.md) |
| 3 | 2026-06-07 20:15 | `05b67ce` | 4 | 6 | 23 | (initial review, same-commit revised below) |
| 3r | 2026-06-07 21:30 | `05b67ce` | 4 | 8 | 27 | [quality-pass/2026-06-07-05b67ce.md](quality-pass/2026-06-07-05b67ce.md) |
| 4 | 2026-06-09 21:16 | `c75c6d1` | 3 | 12 | ~16 | [quality-pass/2026-06-09-c75c6d1.md](quality-pass/2026-06-09-c75c6d1.md) |
| 5 | 2026-06-10 07:27 | `30c0a37` | 1 | 11 | ~17 | [quality-pass/2026-06-10-30c0a37.md](quality-pass/2026-06-10-30c0a37.md) |
| 6 | 2026-06-12 13:13 | `5d64a13` | 2 | ~21 | ~25 | [quality-pass/2026-06-12-5d64a13.md](quality-pass/2026-06-12-5d64a13.md) |
| 7 | 2026-06-12 18:00 | `5e3ffcd` | **0** | ~19 | ~28 | [quality-pass/2026-06-12-5e3ffcd.md](quality-pass/2026-06-12-5e3ffcd.md) |
| 8 | 2026-06-12 19:25 | `53cdf62` ⁽ᵇ⁾ | **0** | ~13 | ~30 | [quality-pass/2026-06-12-53cdf62.md](quality-pass/2026-06-12-53cdf62.md) |
| 9 | 2026-06-15 19:40 | `20ec21a` ⁽ᶜ⁾ | **1** | ~13 | ~22 | [quality-pass/2026-06-15-20ec21a.md](quality-pass/2026-06-15-20ec21a.md) |
| 10 | 2026-06-21 21:31 | `5470212` | **0** | ~12 | ~25 | [quality-pass/2026-06-21-5470212.md](quality-pass/2026-06-21-5470212.md) |
| 11 | 2026-07-15 19:52 | `de9452f` | **2** | ~22 | ~40 | [quality-pass/2026-07-15-de9452f.md](quality-pass/2026-07-15-de9452f.md) |
| 12 | 2026-07-18 08:03 | `1e324d0` | **0** | ~11 | ~16 | [quality-pass/2026-07-18-1e324d0.md](quality-pass/2026-07-18-1e324d0.md) |

⁽ᵇ⁾ Run 8 reviews branch `claude/elastic-pasteur-e2c8c1` (5 fix commits atop `5e3ffcd`), not yet merged to the codex development line.
⁽ᶜ⁾ Run 9 reviews the **codex development line** (`codex/matter-workbench-checkpoint-2026-05-17`, 62 commits atop `5e3ffcd`). Baseline = **Run 7**, not Run 8 — `53cdf62` is a non-ancestor sibling (`merge-base = 5e3ffcd`). The codex line independently re-implemented Run 7's queue, so Run 8's verifications do not transfer; all re-implemented fixes were verified fresh.

Run 10 reviews `main` at `5470212` (the codex line merged to main). Baseline = Run 9.

## Deltas

- **Run 1 → Run 2** (9 commits): 2 P1s cleared (`source-descriptors` resilience, `workspace-service` twin-drift).
- **Run 2 → Run 3** (81 files, ~9800 insertions): 3 P1s cleared (`runtime-db-sql-safety` test added, `matter-status` route-dispatched, `matter-store` dispatch stabilized). 5 new surfaces reviewed. 3 P2s added. Net: **4×P1** (down from 7).
- **Run 3 → Run 3 revised** (same commit, expanded dirty worktree): 6 new surfaces reviewed. `private-beta-feedback-service` P2 (store fragility on malformed ledger) + route-helper triplication P2 (T7 new theme). Net: **+2 P2, +4 P3**. 4×P1 unchanged.
- **Run 3r → Run 4** (50 commits, 166 files, ~17k insertions): 2 P1s cleared (`create-listofdates-engine` monolith decomposed, `upload-service` concurrency serialized). 2 P2s cleared (route-helper triplication, feedback lenient-load). 1 new P1 (`private-beta-users` --password arg). 8 new P2s (skill-store updateStore bug, runtime-db tombstone gap, auth-service hot path, skill-runs maxBuffer, AppContext ref, entries T4 residue, skill-store fingerprint bypass, settings defaults drift). New T8 theme (write-path integrity). Net: **3×P1** (down from 4), **12×P2** (up from 8).
- **Run 4 → Run 5** (2 commits, 9 files — fix-verification delta): 2 P1s cleared (`ai-settings` testConnection redaction, `private-beta-users` argv password rejected), both verified against code + 46/46 tests run. 2 P2s cleared (`updateStore` pre-mutation write on the DB twin, `entries.mjs` Skyline residue). 1 new P2: **F1b** — the updateStore fix landed in one twin only; the filesystem `configurable-skill-store` (default mode) still ignores replacement returns, so the mutator contract now diverges by mode (T7×T8). 1 new P3 (`$HOME` literal defaults remain in deploy script). Run 4's prescribed F1 fix (`result ?? store`) retracted as wrong — implemented `isStoreReplacement` guard is correct. Net: **1×P1** (down from 3), **11×P2**.

- **Run 5 → Run 6** (23 commits, 112 files, ~7,300 insertions — major delta: new mothership telemetry server, telemetry/observability services, per-login authz layer, lawyer-mode UI, superuser panel; 8 parallel reviewers, ~210 tests run, all passing): Run 5's P1 cleared (mtime guard landed + tested; residual downgraded to P2 watch, trigger path possibly unreachable). F1b cleared the right way (`shared/store-mutation.mjs`, both twins import). $HOME-literals P3 cleared beyond prescription. F2 (tombstone) still open and more entrenched (migration 016); F3 (auth sync read) still open and **worse** (now ×3 per API request); write-race still open. **2 new P1s, both T7 twin-drift on the new auth/visibility layer:** F9 — login scoping is DB-twin-only, auth+filesystem combo exposes all matters cross-tenant (`matter-store.mjs:40`); F10 — client operator predicate stricter than server's, default local mode loses the entire Settings surface (`lawyerMode.ts:25-27`). ~13 new P2s led by T9 (telemetry egress without deadlines: no AbortSignal anywhere, inline sync in request paths, retry guard turns one hung socket into permanent silence) and F15 (runtime-db-storage god-module; prepare plans now diverge by storage mode, user-visible). New T10 theme: string-matching tests (SQL-by-regex, React regex-over-source) at exactly the boundaries where the bugs shipped. Net: **2×P1**, **~21×P2**, **~25×P3**.

- **Run 6 → Run 7** (7 commits, 38 files, ~795 insertions — fix-verification delta; 103 tests run across 18 delta-touched files, all passing; tsc clean): **Both P1s cleared, verified beyond prescription.** F9: per-user `activeState()` scoping independent of storage mode + fail-closed startup on auth-without-runtime-index, both prescribed tests landed. F10: `canSeeOperatorSurface(authEnabled, user)` with compiler-enforced threading through all 6 consumers + a behavioral (transpile-and-execute) local-mode test. Run 6's highest-leverage #2 landed: `services/telemetry-sync-client.mjs` de-triplicates sync plumbing and puts `AbortSignal.timeout` (10s) on every egress fetch — F12 cleared as prescribed (dedupe branch queues instead of POSTing); F11 half-cleared (syncs still run inside store-mutation locks, drains hold lock N×10s, no per-tick retry deadline → residual P2). Bonus fix Run 6 missed: materialized-read `return` without `await` inside try/finally deleted the work dir mid-read (`runtime-db-storage-service.mjs:520`), now fixed + regression-tested. New: **N1** feedback sync payloads now carry tester username/displayName/userRole off-VM in safe mode (F22 widened — deliberate but undocumented product call, P2*); **N2** forgiving-feedback contract duplicated client/server (new T7 seed, P3); **N3** sync client has no direct unit test (P3); **N4** deployment-pack Caddyfile clobber (P3). F13/F14/F15/F16/F17/F2/F3 carry — grep-verified still present where files changed orthogonally. Net: **0×P1** (first time since Run 3), **~19×P2**, **~28×P3**.

- **Run 9 → Run 10** (~130 commits on `main` at `5470212`, ~230 source files, ~20k server-side + ~4,720 frontend insertions — major delta: mothership console (auth + store + email + React SPA), credit shadow metering (migration + policy + planner), system health + user readiness, feedback triage, god-module decomposition (11 extracted modules, 2,245→1,030 lines), React UI (nav shell transition, CommandPanel decomposition, SettingsPage decomposition, SkillIdeaSession state machine, readiness gate); 5-reviewer fan-out; **1,594 tests pass at `5470212`, React `tsc -b` clean**). **Largest queue clearance yet: 9 of 11 Run 9 findings cleared, including the lone P1.** P1 queue empty for the first time since Run 7. Cleared: R9-1 (P1 stream crash → `sendRawFileStream` with `pipeline()`), R9-2 (fd leak → same helper), R9-3 (heartbeat lock → providers hoisted), R9-4 (AIza redaction → added to shared + twin), R9-5 (god-module → 11 extracts), R9-6 (materialized write race → path removed), R9-7 (string-tier tests → smoke coverage), R9-8 (preparationErrors.ts → delegates to canonical), R9-9 (citation cross-check → `answerHasUnsupportedRawCitations`). All verified in-code. **New findings are boundary-layer:** mothership console auth (email enumeration oracle R10-1, default-open R10-2, store.mjs redaction bypass R10-3, cookie/crypto duplication R10-4), React UI (dark mode broken R10-5, focus visibility R10-6), DB decomposition (matter.json merge gap R10-7, 5 modules lack unit tests R10-8), pre-existing redaction JSON-quoted gap R10-9. New T12 (default-open boundaries) and T13 (CSS/UI layer discipline) themes. Risk migrated from core services to boundary layers. Net: **0×P1** (cleared), **~12×P2** (9 cleared + ~9 new from new surfaces), **~25×P3.**

- **Run 7 → Run 9** (62 commits on the **codex** line `codex/matter-workbench-checkpoint-2026-05-17`, 145 source files, ~8,302 insertions — major independent-reimplementation delta; 7-reviewer fan-out; **1,319 tests pass at `20ec21a`, React `tsc -b` clean**). Baseline is Run 7, NOT Run 8: the codex line diverged from the Claude branch at `5e3ffcd` and **re-implemented Run 7's queue independently**, so Run 8's verifications were not assumed — each fix verified fresh against codex code via per-file triage (HEAD vs `53cdf62`). **Verdict: shared-module discipline transferred, consumer/boundary discipline did not.** Shared contracts (custody-supersession SQL `017`, `preparation-stages.mjs`, `secret-redaction.mjs`, two-phase `telemetry-sync-client.mjs`) are byte-identical to the verified Claude versions and re-confirmed correct. But **NEW P1: R9-1** — `/api/file-raw` pipes a raw stream with no `error` listener and no process-level guard (rg=0) → **full-server crash on a post-header fs stream error, empirically reproduced on Node 26.3.0**; this is Run 7's `F17` (P2 brittle*) escalated because Run 8's fix was never ported to this line, and **R9-2** (fd leak on client disconnect) makes the EMFILE trigger reachable. Other re-implementations verified: **F2** custody supersession atomic across 4 tables / single deletion entry point (string-tier only → **R9-7/T10**, top follow-through); **F11** two-phase drain clean BUT **R9-3** new heartbeat *capture*-path awaits Postgres providers under a store lock; **F14/R8-1** redactor stragglers carry both directions (**R9-4**: shared redactor lacks `AIza`, local lists lack postgres-URL); **F15** divergence fixed, god-module carries (**R9-5**). New T7 leak: **R9-8** `preparationErrors.ts` is a 3rd divergent redaction path that leaks DSN/bearer/ingestion tokens into text + telemetry. **R9-9** copilot prose not cross-checked against dropped citations. New surfaces (heartbeat svc, zip-intake experiment, RC test-runner) are well-built. 4 suspicions raised + retired (SQL-injection, body-capture, DB-mode crash path, auth cross-tenant). New theme **T11** (streaming/resilience boundary) owns the P1. Net: **1×P1 (re-classified carry), ~13×P2, ~22×P3.**

- **Run 7 → Run 8** (5 commits on `claude/elastic-pasteur-e2c8c1`, 30 files, ~857 insertions — fix-wave over Run 7's queue, authored and then adversarially re-reviewed by the same agent; 1,223 tests passing, tsc clean): **6 P2 rows cleared, all verified in code:** F2 (migration 017 `superseded_at` + tombstone supersession; RLS FOR-ALL verified; *string-tier only — run the real-PG smoke before cutover*), F11r (two-phase sync, no network under store locks, 4-min tick deadline, lock-discipline asserted behaviorally), F13 (pool error handler, fake-pool tested), F15's user-visible half (shared `preparation-stages.mjs`, real DB-mode metadata/warnings, cross-mode parity test), F17 (headersSent + stream listener — sabotage-verified: test hangs without the fix), F14 → P3 (7 copies delegated incl. React twin; **3 stragglers found contradicting the fix's own commit message**: observability:207, job-status:266, ui-hardening-pass:434 — the last knows `AIza` keys the shared list doesn't). P3s cleared: creds 0600, CLI exit 1, sync-client tests (N3). New: **R8-1** redactor stragglers (P3) · **R8-2** raw-stream fd retention on client disconnect (P3, 1-liner) · R8-3 feedback now persists before sync (improvement, noted). Two suspicions raised against the new code and retired with evidence (RLS, atomic ledger writes). Net: **0×P1**, **~13×P2**, **~30×P3**.

- **Run 10 → Run 11** (~130 commits on `main` at `de9452f`, ~640 files, ~68,300 insertions — the largest delta yet: MW List of Dates workflow, Case Timeline canonicalization, procedural posture diagnosis, source removal/custody, first-class upload sessions (migration 024), copilot web research, native skill runner, Mothership signal lifecycle, legal source sidecar; 9-reviewer fan-out, both P1s lead-verified in code; **1,938 tests pass at `de9452f`, React `tsc -b` clean**). **Carried queue: 11 of 12 P2-level items cleared, all verified** — R10-1 (fire-and-forget + throttle refund), R10-2 (auth fail-closed), R10-3 (shared `httpError`), R10-4 (`shared/auth-primitives.mjs`), R10-5/R10-6 (dark mode + focus catch-all, pinned by new a11y test), R10-9 (redaction value class, both twins), F20 (active-release guard, test-pinned), R9-10, plus P3s a/d/e/h/n/r. R10-8 partial (2 of 5 test files landed). **But 2 new P1s, both on replay paths:** **R11-1** — DB source removal gates its custody-event insert on "document exists" not "document transitioned" (`source-removal-mutation-service.mjs:246-272,316`), so replaying a removal on an already-inactive document fabricates a `source_file.removed_from_active_record` event in the legal custody ledger and clobbers currentness, while reporting `already_inactive`; the FS twin correctly no-ops; client mints a fresh idempotency key per click. **R11-2** — `commitUploadSession` queues by session id while `addUploadedFilesToMatter` queues by matter id (`runtime-db-storage-service.mjs:498` vs `:286`), deserializing same-matter uploads and reopening R10-7's cross-transaction matter.json lost-intake race; commit lock evaporates pre-work, cancel-vs-commit races end "committed". **New theme T14 (sibling-path bypass)** generalizes T7: the guard exists, the second path skips it — research mode bypasses Ask's citation guard (R11-19), retry executes against a different matter than recorded (R11-4), closed signals never reopen on recurrence (R11-9), commit-retry never re-enqueues extract (R11-15). N1 **widened** (username+userRole now off-VM in safe mode, R11-10). ~22 new P2s across the six new surfaces. Net: **2×P1, ~22×P2, ~40×P3.**

- **Run 11 → Run 12** (21 commits on `main` at `1e324d0`, 215 files, +4,325/−20,909 — a deliberate **fix cycle** over Run 11's queue plus the legacy-frontend retirement; 6-family reviewer fan-out, every carried verdict re-verified in code; **1,766 tests pass at `1e324d0`** (legacy suites deleted with `frontend/`), React `tsc -b` clean). **Carried queue: 13 of 26 cleared — including both P1s, proven by a new executed-Postgres race/replay suite** (`integration-test/runtime-db-replay-concurrency.postgres.mjs`: disposable DB, restricted `nosuperuser/nobypassrls` role, two independent service instances). Cleared: R11-1 (all three removal gates key on `updated_document`; replay asserted non-clobbering), R11-5 (two-layer race-safe dedup), R11-6 (fails closed ×5 shapes), R11-8 (shared preview builder), R11-9 (recurrence reopens; vocab centralized in `shared/private-beta-signal-lifecycle.mjs`), R11-10 (identity gated to firm_internal + retroactive scrub — N1 settled after 5 runs), R11-11 (partial unique index + preflight + 409, race-tested), R11-12 (attempt-guarded claim + fail-closed sweep), R11-15, R11-19 (research runs Ask's full guard stack, behavioral twin test), R11-23 (golden eval reads Case Timeline + is itself e2e-tested), R11-24 (schema-derived validator + real rejection test), R11-26 (`frontend/` deleted). Partial: R11-2 (matter-keyed queue + advisory lock landed + race-tested, but commit UPDATE still has no status predicate — T15), R11-4 (409-on-mismatch landed; visibility half open), R11-7 (resume path built but unreachable — client mints fresh idempotency keys; still no doctor tooling), R11-13/14/18/21. Not fixed: R11-3, R11-16, R11-17, R11-20, R11-22, R11-25. **New: 0×P1, 3 new P2** — **R12-1** story-worker `persistMatterJson` skips the new lock (R11-2's lost-intake race through a different door), **R12-2** enqueue upsert never resets terminal rows so the commit-retry re-enqueue silently no-ops in exactly the user's wedged-retry scenario, **R12-3** react reconnect hardening pinned by regex-over-source tests (~49 asserts execute nothing) right after 19.8k lines of behavioral legacy coverage were deleted. **New theme T15 (single-door invariants):** enforcement lives in caller discipline, not the data layer (lock-only commit UPDATE, terminal-row upsert, default-open receipts, SET_CONFIG). T14 receded a level: the residue is the retry-of-the-retry, the bookkeeping-of-the-reopen (stale `operatorStatus` attribution R12-6). T10 split by surface: server-side evidence jumped a tier; client-side fell one. Net: **0×P1, ~11×P2, ~16×P3.**

## Open P1 queue (as of Run 12)

**Empty** — both Run 11 P1s verified cleared in code with executed-Postgres evidence.

## Open P2 queue (as of Run 12)

**R12-1** story-worker matter.json write outside `withSerializedMatterWrite` (`server.mjs:789-802`, `matter-workflow-routes.mjs:1374-1389`) · **R12-2** enqueue upsert no-ops on terminal job rows, defeating the commit-retry re-enqueue (`runtime-db-processing-job-store.mjs:41-45`) · **R12-3** react-ui regex-over-source test tier on the reconnect/identity logic (`test/react-auto-preparation.test.mjs`) · **R11-2 residual** commit UPDATE lacks status predicate/row assertion; vestigial FOR UPDATE · **R11-7 residual** client idempotency key unstable → mid-mutation 409-lockout unreachable-resume; no doctor repair tooling · **R11-16** multipart eager heap buffering (now pure waste — 1-line fix) · **R11-17** no session reaper; cancel doesn't reclaim intake · **R11-18 residual** telemetry awaited on prep critical path; zero AbortSignal in react-ui · **R11-3** matter-workflow-routes god-module + ~400 dead legacy lines · **R11-25** App/MatterOverview guarded-run duplication · **R11-20/21/22** operator-error boundary / MW LOD truncation (announced, not fixed) / soften-language rewrites · **F16/F3/F19** auth boundary, parked for dedicated security pass (7 runs) · **R11-27** technical-file classifications (not re-verified; likely 2 schemes post-retirement) · **R10-8 residual** (doctor-fix, extract-service, matter-init untested)
