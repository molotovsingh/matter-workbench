# Matter Workbench v1.0.0-beta.132 — Timeline Recovery

Date: 2026-07-16

Status: Current supervised private beta release

## Release Position

Release as:

```text
v1.0.0-beta.132
```

Tag target / deployed commit:

```text
1647f3c Match queued runtime jobs by backend id
```

The annotated tag points to the exact application commit installed on the DigitalOcean beta VM. Release documentation is recorded on `main` after the immutable tag target.

## Included Since v1.0.0-beta.129

- Gives Case Timeline a dedicated 9,000-token output budget instead of inheriting the smaller shared provider ceiling.
- Distinguishes truncated and incomplete provider responses with stable fail-closed errors and never persists partial legal output.
- Replaces raw browser network failures with reconnect guidance, preserves last-known preparation progress, and matches queued runtime jobs by their backend identity.
- Keeps native skills available when stale configurable records reuse promoted native slash commands.
- Deduplicates active native-skill runs and preserves caller idempotency keys.
- Adds real PostgreSQL replay, upload-race, and concurrent active-matter-name coverage; migration `026_active_matter_name_uniqueness` enforces the matching database invariant.
- Hardens upload-session persistence, processing-job leases, source-removal replay/currentness, citation validation, and telemetry privacy.
- Retires the unreachable plain-JS browser tree; React remains the only browser product.
- Keeps canonical workspace folder names visible: `00_Inbox`, `10_Library`, `20_Workshop`, `30_Drafts`, and `40_Dispatch`.

## Live Deployment Evidence

Deployment surface:

```text
https://mwb-beta.139.59.74.9.sslip.io
```

Verified deployment facts:

- VM `current` points to `/home/aks/matter-workbench-deployments/1647f3c`.
- Runtime and Mothership services are active.
- Runtime database migration `026_active_matter_name_uniqueness` is applied; its duplicate-name preflight found zero conflicting groups.
- VM-local service check passed with runtime PostgreSQL enabled, 17 visible matters, readable workspace/file preview, and user readiness `ready`.
- Mothership console check passed.
- Rendered private-beta UI hardening passed 12/12 checks with zero console errors.
- UI hardening evidence: `/home/aks/matter-workbench-backups/ui-hardening/private-beta-ui-hardening-2026-07-16T12-08-21-712Z/ui-hardening-report.md`.
- Live acceptance on `Rekha v Ashvarya Garg` regenerated Case Timeline successfully in one processing attempt: 85 entries, `list-of-dates/v1`, recorded output budget 9,000, with verified JSON/CSV/Markdown custody objects.
- The same preparation chain generated Matter Story successfully. A separate downstream procedural-posture response was truncated after retries and failed closed; no partial posture artifact was saved.

Release byte stamped on the running service:

```text
MWB_RELEASE_COMMIT=1647f3c
MWB_RELEASE_VERSION=v1.0.0-beta.132
MWB_RELEASE_CODENAME=Timeline Recovery
MWB_RELEASE_LABEL=Case Timeline reliability
MWB_RELEASE_DATE=2026-07-16
```

## Validation

- `npm test` — 1,766 tests passed.
- `npm run ui:build` — React typecheck and production build passed.
- `MWB_POSTGRES_TEST_ADMIN_URL='postgresql:///postgres' npm run test:postgres` — real PostgreSQL replay/concurrency suite passed against a disposable database and role.
- Isolated React UI smoke — 70/70 checks passed.
- `git diff --check` passed.
- Live migration, service, Mothership console, rendered UI hardening, and affected-matter Case Timeline acceptance passed as recorded above.

## Not Promised

- This remains a supervised private beta, not a production-readiness claim.
- Provider availability and completion are not guaranteed. Invalid, incomplete, or truncated structured output continues to fail closed.
- The downstream procedural-posture truncation observed during live acceptance is not claimed fixed by this release; it produced no partial artifact and requires a focused follow-up before rerun.
- The release does not replace application transactions, advisory locks, idempotency, replay checks, or custody controls with a generic durable-execution framework.
- Source removal remains removal from the active record, not physical deletion of retained source bytes.

## Operator Notes

- Immediate rollback target: `9c57fef` (`v1.0.0-beta.131` deployment checkpoint).
- Official prior supervised-beta rollback target: annotated `v1.0.0-beta.129` at `696ca35`.
- Use `npm run private-vm:rollback -- --to <commit>` with the normal VM host/user options; do not move release tags.
- If Case Timeline reports `provider.output_truncated` or `provider.incomplete_response`, keep the existing artifact unchanged and rerun only after checking the task-specific output budget and provider health.
- Do not rerun the failed procedural-posture step merely to clear the status; investigate its finalizer output size first.
