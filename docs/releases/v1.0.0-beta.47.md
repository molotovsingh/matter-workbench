# Matter Workbench v1.0.0-beta.47

Date: 2026-06-25

Codename: **Chronology Engine**

This is a Beta 3 internal architecture release. It completes the LLM-side AI
provider service migration by routing one-pass and two-pass List of Dates
providers through the first-class provider boundary.

## Release Position

Release as:

```text
v1.0.0-beta.47
```

Tag target / deployed commit:

```text
bba52fe Route List of Dates providers through AI provider service
```

The tag points to the exact code installed on the DigitalOcean beta VM.

## Included Since beta.46

AI provider boundary continuation:

- migrated one-pass `/create_listofdates` provider calls through
  `services/ai-provider-service.mjs`;
- migrated two-pass List of Dates pass 1 candidate generation through the same
  boundary;
- migrated two-pass List of Dates pass 2 editor generation through the same
  boundary;
- preserved OpenAI Responses and OpenRouter request shapes for List of Dates,
  including strict JSON schema output, no OpenRouter temperature field, and
  source-backed analysis metadata merge behavior;
- preserved direct provider factory exports as compatibility wrappers;
- preserved runtime DB List of Dates behavior and the two-pass feature gate.

## Live Deployment Evidence

The DigitalOcean beta is deployed at:

```text
https://mwb-beta.139.59.74.9.sslip.io
```

Verified deployment facts:

- deployed `current` symlink points to
  `/home/aks/matter-workbench-deployments/bba52fe`;
- rollback candidate recorded by the ops pack: `3ee99fe`;
- runtime and mothership user services are active;
- runtime DB migrations were checked during deploy;
- private VM service check passed;
- user readiness is `ready`;
- user-facing language leak check: `no`;
- rendered private beta UI hardening pass completed 12/12 checks with zero
  console errors;
- private VM ops pack passed with healthy logs and rollback evidence.

Release byte stamped on the running service:

```text
MWB_RELEASE_COMMIT=bba52fe
MWB_RELEASE_LABEL=AI provider service List of Dates migration
MWB_RELEASE_DATE=2026-06-25
MWB_RELEASE_NOTE=Routes one-pass and two-pass List of Dates providers through the first-class AI provider service.
```

Latest evidence paths:

```text
/home/aks/matter-workbench-backups/ui-hardening/private-beta-ui-hardening-2026-06-25T03-11-34-985Z/ui-hardening-report.md
/home/aks/matter-workbench-backups/ops-packs/private-vm-ops-pack-2026-06-25T03-11-54-185Z/ops-pack.md
```

## Validation

Local validation before tagging:

```text
npm test --silent
npm run ui:typecheck --silent
npm run ui:build --silent
npm run console:build --silent
git diff --check
```

Result:

```text
full test suite: 1700/1700 passing
ui:typecheck: pass
ui:build: pass
console:build: pass
git diff --check: pass
```

Deployment validation:

```text
npm run private-vm:rsync-deploy -- --host 139.59.74.9 --user aks --commit bba52fe --base-url https://mwb-beta.139.59.74.9.sslip.io --deployment-root /home/aks/matter-workbench-deployments --release-label "AI provider service List of Dates migration" --release-date 2026-06-25 --release-note "Routes one-pass and two-pass List of Dates providers through the first-class AI provider service."
```

Result:

```text
deploy: success
service_check: ok
ui_hardening: 12/12
console_errors: 0
ops_pack: success
current_release: bba52fe
rollback_candidate: 3ee99fe
```

## Not Promised

This release does not migrate OCR, provider-run ledger recording, AI settings,
or startup checks.

## Operator Notes

No user-facing workflow should change. If one-pass or two-pass List of Dates show
provider-related regressions, rollback to `3ee99fe`.
