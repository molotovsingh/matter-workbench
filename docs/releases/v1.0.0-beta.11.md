# Matter Workbench v1.0.0-beta.11

Date: 2026-06-15

Codename: **Public Hearth**

This was the first tagged Beta 3 public-private deployment checkpoint.

It moved the beta line beyond the named-tester-account release by adding the
runtime DB and public-VM hardening needed for real trusted-lawyer testing:

- DB-backed private beta runtime as the normal deployed path;
- safer upload handling for lawyer-style matter names and duplicate paths;
- private beta tester feedback and telemetry surfaces;
- repeated VM release/deploy evidence tooling;
- Skill Factory provider-failure telemetry for quota and upstream failures;
- rendered UI hardening as a repeatable command.

## Release Position

Release as:

```text
v1.0.0-beta.11
```

Tag target:

```text
5d10ca7 Track skill sample provider quota failures
```

This tag is retained as a historical Beta 3 marker. It is no longer the current
private beta checkpoint.

## Not Promised

This release did not promise public SaaS signup, cloud object storage,
unsupervised legal reliance, or self-service account recovery. It remained a
supervised private beta for trusted testers.
