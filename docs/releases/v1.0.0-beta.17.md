# Matter Workbench v1.0.0-beta.17

Date: 2026-06-17

This is the Beta 3 diagnostic-hardening marker after closing the remaining
uncoded local/configurable-skill diagnostic gaps.

## Release Position

Release as:

```text
v1.0.0-beta.17
```

Tag target / deployed commit:

```text
618768e Add upload service error codes
```

Branch documentation commits after `618768e` record this release note and
current-release pointers only; they do not change the hosted product behavior
covered by the beta.17 tag.

Meaning:

- the deployed private beta remains the React-only product UI;
- runtime DB storage remains the accepted hosted-beta path;
- DigitalOcean deployment uses the repeatable `private-vm:rsync-deploy` flow;
- runtime and mothership services are expected to be active;
- configurable-skill creation/lifecycle/run/provider diagnostics have stable
  operator codes;
- local matter-store and upload-service orchestration diagnostics have stable
  operator codes;
- no uncoded `makeHttpError(...)` calls remain in tracked `.mjs` files;
- credit metering remains shadow/report-only, with no billing or enforcement;
- no public SaaS signup, self-service billing, or unsupervised legal reliance is
  implied.

## Included Since beta.16

Diagnostic hardening added stable operator-facing codes for:

- configurable skill definition validation and provider API-key requirements;
- configurable skill lifecycle controls and custom-skill run selection failures;
- configurable skill creation-pipeline validation/model-configuration failures;
- Skill Factory route-level matter-selection and overlap guards;
- local/runtime matter-store selection and storage-folder resolution failures;
- local upload-service matter collisions, matter selection, and path guards.

The diagnostic contract is documented in
[`docs/contracts/diagnostic-surfaces.md`](../contracts/diagnostic-surfaces.md).

## Live Deployment Evidence

The DigitalOcean beta was deployed from commit `618768e` to:

```text
https://mwb-beta.139.59.74.9.sslip.io
```

Verified closeout facts for this marker:

- deployed `current` symlink points to
  `/home/aks/matter-workbench-deployments/618768e`;
- runtime and mothership user services are active;
- runtime DB migrations were checked and already applied through
  `019_credit_ledger`;
- private VM service check passed after deploy;
- rendered private beta UI hardening pass completed 11/11 checks with zero
  console errors;
- VM-local authenticated smoke confirmed:
  - `runtime_db_enabled: yes`;
  - `matter_count: 6`;
  - workspace and file preview readable;
- recent runtime and mothership logs showed no obvious fatal/unhandled/error
  output during closeout inspection;
- each diagnostic slice passed focused tests, full `npm test --silent`,
  `npm run ui:typecheck --silent`, and `npm run ui:build --silent` before
  deploy.

Latest remote UI evidence path recorded by the deploy script:

```text
/home/aks/matter-workbench-backups/ui-hardening/private-beta-ui-hardening-2026-06-17T12-28-56-384Z/
```

## Remaining Known Diagnostic Gaps

A tracked-code scan found no uncoded `makeHttpError(...)` calls in `.mjs` files
at this marker. Future diagnostic work should be driven by new surfaces,
operator feedback, or broader product prioritization rather than by the prior
uncoded-site backlog.

## Not Promised

This release still does not promise public SaaS signup, self-service password
reset, unsupervised legal reliance, external object storage, hard credit
enforcement, or a multi-region production posture. It is a private-cloud beta
for trusted testers.

## Operator Notes

For this release line, deploy the exact commit with:

```bash
npm run private-vm:rsync-deploy -- \
  --host 139.59.74.9 \
  --user aks \
  --deployment-root /home/aks/matter-workbench-deployments \
  --commit 618768e \
  --base-url https://mwb-beta.139.59.74.9.sslip.io \
  --release-label "Beta 3 diagnostic hardening" \
  --release-date 2026-06-17 \
  --release-note "Stable local upload-service orchestration diagnostic codes."
```
