# Documentation Archive

This folder keeps superseded planning notes out of the active documentation
surface without deleting their history.

Archived docs are historical context only. Before using one as a guide, check
the current runtime code and active docs at the repo root or under `docs/`.

## Batches

- `2026-05-13/` - pre-runtime planning contracts retired after the guarded
  `/prepare_matter` workflow and configurable skill factory became runnable.
