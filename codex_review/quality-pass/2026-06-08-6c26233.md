# Matter Workbench - Engineering Quality Pass AKS - 6c26233

> Report: 2026-06-08 08:06 IST
> Branch: codex/matter-workbench-checkpoint-2026-05-17
> Commit: 6c26233
> Worktree: clean tracked source worktree; `codex_review/` remains local and untracked
> Scope: currentness refresh after runtime DB hardening

This is an engineering-quality pass, not a security audit. It re-checks the
prior Codex quality-pass findings against the current HEAD and looks for new
current P1/P2 residue in the same runtime DB, private-beta, and skill-run
surfaces.

## Result

P0 queue: none found.

P1 queue: none currently open from this pass.

P2 queue: none currently open from this pass.

The prior report at `32646a5` is now stale. Its P1/P2/P3 findings were useful,
but they have been closed by the subsequent hardening commits listed below.

## Prior Findings Rechecked

| Prior finding | Prior severity | Current status | Closing evidence |
| --- | --- | --- | --- |
| Runtime DB add-files allocation was read-before-write and could race | P1 | Closed | `services/runtime-db-storage-service.mjs` now locks the target `matters` row with `FOR UPDATE` and reserves `next_file_number` inside the transaction. Covered by `runtime DB storage service reserves add-files allocation under a matter row lock`. |
| Materialized runtime DB writes did not tombstone deleted files | P2 | Closed | `runMaterializedMatterWrite` now computes deleted paths and writes `deleted_pending` tombstones for matching `storage_objects`, also marking affected `matter_artifacts` not current. Covered by `runtime DB storage service tombstones materialized artifacts deleted by a workflow`. |
| Intake and extraction artifacts used direct writes for critical structured state | P2 | Closed | `matter-init-engine.mjs` and `extract-engine.mjs` now import and use `writeFileAtomic`; the hygiene test forbids direct `await writeFile(...)` for those files. |
| Runtime DB configurable skill catalog lacked a DB-level concurrency guard | P2 | Closed | `services/runtime-db-configurable-skill-store.mjs` now uses an advisory transaction lock plus a catalog fingerprint guard. Covered by stale-catalog conflict tests. |
| Runtime DB skill-run updates depended only on an in-process `transientRuns` map | P3 | Closed | `services/runtime-db-configurable-skill-runs-service.mjs` now falls back to reading the persisted run from Postgres before updating. Covered by `runtime DB configurable skill run update recovers persisted runs after process restart`. |

## Current Unit Map

| Unit | Current read |
| --- | --- |
| `services/runtime-db-storage-service.mjs` | Still the largest and riskiest bridge file, but the open correctness issues from the prior pass are now tested and closed. The remaining risk is size/bridge complexity, not a fresh P1/P2 bug found in this pass. |
| `services/runtime-db-configurable-skill-store.mjs` | The catalog is guarded by DB locking and fingerprint checks. This is enough for current private-beta runtime expectations. |
| `services/runtime-db-configurable-skill-runs-service.mjs` | Run receipts remain backend-owned and DB-backed updates can recover persisted rows. |
| `services/private-beta-feedback-service.mjs` | Malformed historical feedback rows are skipped on read; new writes remain strict. Sync tests cover safe and firm-internal modes. |
| `services/private-beta-signal-service.mjs` | Diagnostic capture is intentionally non-blocking and deduped. This should not be treated as product-critical state. |
| `routes/route-utils.mjs` | The repeated `usesRuntimeDbStorage` / `safeCaptureBetaSignal` helpers have been extracted; route duplication from the earlier review no longer exists. |

## Verification

Focused tests run during this pass:

```text
node --test \
  test/runtime-db-storage-service.test.mjs \
  test/runtime-db-configurable-skill-store.test.mjs \
  test/runtime-db-configurable-skill-runs-service.test.mjs \
  test/private-beta-feedback-service.test.mjs \
  test/private-beta-signal-service.test.mjs
```

Result: 39 tests passed, 0 failed.

These tests directly cover the closed runtime DB add-files lock, materialized
delete tombstones, runtime DB skill catalog stale-write guard, runtime DB
skill-run persisted update recovery, private beta feedback lenient historical
load, and private beta signal capture/sync behavior.

## Watch Items

These are not current P1/P2 findings, but they remain useful engineering radar.

| Watch item | Why it remains watch-level |
| --- | --- |
| `services/runtime-db-storage-service.mjs` is 1,800+ lines | It translates between Postgres custody and the existing materialized filesystem engine path. The file is large because the bridge is doing real work. Split only when a natural seam appears; do not mechanically refactor during release hardening. |
| Runtime DB access still goes through `psql` subprocess helpers | This is acceptable for the current private VM beta path and is guarded by safe-role checks. A direct pooled Postgres client is the natural later production implementation, not a blocker for the current release slice. |
| Private-beta diagnostic capture swallows capture errors | This is deliberate: diagnostic capture must never break the product route that exposed the signal. Dedicated observability can grow later if signal loss becomes operationally meaningful. |
| `codex_review/` is local and untracked | This is intentional so Codex review artifacts do not collide with other review artifacts or release source commits unless explicitly requested. |

## Recommended Next Target

Move from code hardening to private-beta deployment repeatability:

1. Run the private VM release gate scripts against the current build.
2. Confirm browser access, auth, feedback sync, diagnostic signal capture, DB runtime smoke, and recoverability pack on the VM.
3. Fix only failures that appear in those gates.

That is now higher leverage than another speculative code refactor because the
previous runtime DB correctness queue has been closed.
